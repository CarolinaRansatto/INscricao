<% provide(:title, 'Criar Admin') %>
<div class="container">
    <div class="col-sm-4 col-sm-offset-4">
		<h1>Criar Novo Admin</h1>

		<%= form_for @admin do |a| %>
		    <%= render 'shared/error_messages', object: f.object %>
			<%= f.label :email%>
			<%= f.text_field :email, :class=>'form-control' %>
			
			<%= f.submit "Cadastrar", :class=>'btn btn-primary center-block' %>
		<% end %>
	</div>
</div>