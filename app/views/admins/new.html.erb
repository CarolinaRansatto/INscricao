<% provide(:title, 'Criar Admin') %>

<h1>Criar Novo Admin</h1>

<%= form_for @admin do |a| %>
    <%= render 'shared/error_messages', object: f.object %>
	<%= f.label :email%>
	<%= f.text_field :email %>
	
	<%= f.submit "Cadastrar" %>
<% end %>