<% provide(:title, 'Processo Seletivo') %>

<h1>Inscreva-se no Processo Seletivo</h1>

<%= form_for (@candidato) do |f| %>
    <%= render 'shared/error_messages', object: f.object %>
	<%= f.label :data %>
	<%= f.text_field :data %>

	<%= f.submit "Inscrever" %>
<% end %>